{
  "name": "company",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "npx serve . --listen 3000",
    "dev": "npm run serve",
    "prepare": "git config core.hooksPath .githooks"
  }
}
